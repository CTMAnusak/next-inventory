# คู่มือการลบสาขาและระบบ Snapshot ✅

## สรุปการเปลี่ยนแปลง

✅ **ระบบลบสาขาได้รับการปรับปรุงแล้ว** - ตอนนี้สามารถลบสาขาได้แม้มีผู้ใช้อยู่ โดยระบบจะ snapshot ชื่อสาขาล่าสุดก่อนลบเสมอ

---

## 🎯 วัตถุประสงค์

เมื่อลบสาขา ระบบจะ:
1. **Snapshot ชื่อสาขาล่าสุด** ในทุกตารางที่ใช้ข้อมูลสาขานั้น
2. **อนุญาตให้ลบได้** แม้มีผู้ใช้หรือข้อมูลอ้างอิงอยู่
3. **คงชื่อสาขาไว้ในประวัติ** เพื่อให้สามารถดูประวัติได้แม้สาขาถูกลบแล้ว
4. **ข้อมูลอื่นๆ ยังอัพเดตได้ปกติ** (ชื่อผู้ใช้, อุปกรณ์, หมวดหมู่, แอดมิน)

---

## 📋 กระบวนการลบสาขา

### ขั้นตอนที่ 1: Snapshot ชื่อสาขา

เมื่อลบสาขา ระบบจะ snapshot ชื่อสาขาล่าสุดในตารางต่อไปนี้:

1. **User Collection** - ผู้ใช้ที่ใช้สาขานี้
2. **RequestLog** - ประวัติการเบิกอุปกรณ์
3. **ReturnLog** - ประวัติการคืนอุปกรณ์
4. **IssueLog** - ประวัติการแจ้งปัญหา
5. **InventoryItem** - อุปกรณ์ที่มีข้อมูลสาขา
6. **DeletedUser** - ผู้ใช้ที่ถูกลบแล้วแต่ยังมีข้อมูลสาขา

**ไฟล์:** `src/lib/office-snapshot-helpers.ts` - `snapshotOfficeBeforeDelete()`

```typescript
// Snapshot ชื่อสาขาในทุกตาราง
const snapshotResult = await snapshotOfficeBeforeDelete(id);
// ผลลัพธ์: { success: true, updated: { users: 5, requestLogs: 10, ... } }
```

### ขั้นตอนที่ 2: อัพเดต officeId

หลังจาก snapshot แล้ว ระบบจะอัพเดต `officeId` ให้ชี้ไปที่ `UNSPECIFIED_OFFICE` (ไม่ระบุสาขา) แต่**คงชื่อสาขาไว้** (จาก snapshot)

**ไฟล์:** `src/app/api/admin/offices/[id]/route.ts` (บรรทัด 167-174)

```typescript
// ⚠️ สำคัญ: อัพเดตเฉพาะ officeId เท่านั้น ไม่ต้องอัพเดต office/officeName 
// เพราะ snapshotOfficeBeforeDelete ได้บันทึกชื่อสาขาไว้แล้ว
await Promise.all([
  User.updateMany({ officeId: id }, { $set: { officeId: DEFAULT_OFFICE_ID } }),
  RequestLog.updateMany({ requesterOfficeId: id }, { $set: { requesterOfficeId: DEFAULT_OFFICE_ID } }),
  // ... และอื่นๆ
]);
```

### ขั้นตอนที่ 3: Soft Delete

ทำการ soft delete สาขา (ตั้งค่า `isActive = false` และ `deletedAt`)

```typescript
office.isActive = false;
office.deletedAt = new Date();
await office.save();
```

---

## 🔍 ตัวอย่างการทำงาน

### สถานการณ์: ลบสาขา "สยาม" ที่มีผู้ใช้อยู่

**ก่อนลบ:**
- สาขา: `OFFFHRROVO5` - "สยาม"
- ผู้ใช้: 3 คน
- ประวัติเบิก: 15 รายการ
- ประวัติคืน: 10 รายการ

**หลังลบ:**

1. **Snapshot ข้อมูล:**
   - ชื่อสาขา "สยาม" ถูกบันทึกใน:
     - `User.office` = "สยาม" (3 รายการ)
     - `RequestLog.requesterOffice` = "สยาม" (15 รายการ)
     - `ReturnLog.returnerOffice` = "สยาม" (10 รายการ)

2. **อัพเดต officeId:**
   - `User.officeId` = `UNSPECIFIED_OFFICE` (แต่ `User.office` ยังเป็น "สยาม")
   - `RequestLog.requesterOfficeId` = `UNSPECIFIED_OFFICE` (แต่ `RequestLog.requesterOffice` ยังเป็น "สยาม")
   - `ReturnLog.returnerOfficeId` = `UNSPECIFIED_OFFICE` (แต่ `ReturnLog.returnerOffice` ยังเป็น "สยาม")

3. **ผลลัพธ์:**
   - ✅ ประวัติยังแสดงชื่อสาขา "สยาม" ได้
   - ✅ ผู้ใช้ถูกย้ายไปใช้สาขา "ไม่ระบุสาขา" (สำหรับการใช้งานใหม่)
   - ✅ ข้อมูลอื่นๆ (ชื่อผู้ใช้, อุปกรณ์, หมวดหมู่) ยังอัพเดตได้ปกติ

---

## 📊 ตารางที่ได้รับผลกระทบ

| ตาราง | ฟิลด์ที่ Snapshot | ฟิลด์ที่อัพเดต |
|-------|------------------|----------------|
| **User** | `office`, `officeName` | `officeId` → `UNSPECIFIED_OFFICE` |
| **RequestLog** | `requesterOffice`, `requesterOfficeName` | `requesterOfficeId` → `UNSPECIFIED_OFFICE` |
| **ReturnLog** | `returnerOffice`, `returnerOfficeName` | `returnerOfficeId` → `UNSPECIFIED_OFFICE` |
| **IssueLog** | `office`, `officeName` | `officeId` → `UNSPECIFIED_OFFICE` |
| **InventoryItem** | `requesterInfo.office`, `requesterInfo.officeName` | `requesterInfo.officeId` → `UNSPECIFIED_OFFICE` |
| **DeletedUser** | `office`, `officeName` | `officeId` → `UNSPECIFIED_OFFICE` |

---

## ✅ ข้อมูลที่ยังอัพเดตได้ปกติ

หลังลบสาขาแล้ว ข้อมูลต่อไปนี้**ยังคงอัพเดตได้ตามปกติ**:

- ✅ **ชื่อผู้ใช้** (firstName, lastName, nickname)
- ✅ **แผนก** (department)
- ✅ **อุปกรณ์** (itemName, serialNumber, status, condition)
- ✅ **หมวดหมู่** (category)
- ✅ **แอดมิน** (approvedBy, processedBy)
- ✅ **สถานะ** (status, approvalStatus)
- ✅ **สภาพ** (condition)

**เฉพาะชื่อสาขาเท่านั้น**ที่จะถูก snapshot และไม่เปลี่ยนแปลงอีก

---

## 🚫 สิ่งที่ป้องกันการลบ

ระบบจะ**ไม่อนุญาตให้ลบ**ในกรณีต่อไปนี้:

1. **System Office** (`isSystemOffice = true`)
   - เช่น "ไม่ระบุสาขา" (UNSPECIFIED_OFFICE)
   - ข้อความ: "ไม่สามารถลบ System Office ได้"

---

## 🎨 การแสดงผลใน Frontend

เมื่อลบสาขาสำเร็จ ระบบจะแสดงข้อความ:

- **มีข้อมูลที่ snapshot:** `"ลบสาขาสำเร็จ (ได้ snapshot ข้อมูล X รายการ)"`
- **ไม่มีข้อมูล:** `"ลบสาขาสำเร็จ"`

**ไฟล์:** `src/components/OfficeManagementModal.tsx` (บรรทัด 87-113)

---

## 📝 ไฟล์ที่แก้ไข

1. **`src/app/api/admin/offices/[id]/route.ts`**
   - ลบการตรวจสอบ `checkOfficeUsage()` ที่ป้องกันการลบ
   - เพิ่มการ snapshot ก่อนลบเสมอ
   - ปรับการอัพเดตให้คงชื่อสาขาไว้ (ไม่เขียนทับ)

2. **`src/components/OfficeManagementModal.tsx`**
   - ปรับปรุงการแสดงผลข้อความสำเร็จให้รวมข้อมูล snapshot
   - ลบการแสดงข้อความ error สำหรับ usage (เพราะตอนนี้ลบได้เสมอ)

---

## 🔄 ความแตกต่างจากเดิม

### ❌ เดิม (ก่อนแก้ไข)

```typescript
// ตรวจสอบว่ามีการใช้งานหรือไม่
const usage = await checkOfficeUsage(id);

if (usage.isUsed) {
  return NextResponse.json({
    error: 'ไม่สามารถลบสาขาได้ เนื่องจากมีการใช้งานอยู่',
    usage: usage.usage
  }, { status: 400 });
}

// Snapshot (จะมาถึงที่นี่ได้เฉพาะเมื่อไม่มี usage)
const snapshotResult = await snapshotOfficeBeforeDelete(id);

// อัพเดต (เขียนทับชื่อสาขาเป็น "ไม่ระบุสาขา")
await User.updateMany({ officeId: id }, { 
  $set: { 
    officeId: DEFAULT_OFFICE_ID, 
    office: 'ไม่ระบุสาขา',  // ❌ เขียนทับ snapshot
    officeName: 'ไม่ระบุสาขา' 
  } 
});
```

**ปัญหา:**
- ❌ ไม่สามารถลบได้ถ้ามีผู้ใช้อยู่
- ❌ Snapshot ถูกเขียนทับด้วย "ไม่ระบุสาขา"

### ✅ ใหม่ (หลังแก้ไข)

```typescript
// Snapshot ก่อนลบเสมอ (แม้มีผู้ใช้อยู่)
const snapshotResult = await snapshotOfficeBeforeDelete(id);

// อัพเดตเฉพาะ officeId (คงชื่อสาขาไว้)
await User.updateMany({ officeId: id }, { 
  $set: { 
    officeId: DEFAULT_OFFICE_ID  // ✅ อัพเดตเฉพาะ officeId
    // ✅ office และ officeName ยังคงเป็นชื่อสาขาเดิม (จาก snapshot)
  } 
});
```

**ข้อดี:**
- ✅ ลบได้แม้มีผู้ใช้อยู่
- ✅ Snapshot ชื่อสาขาไว้ในประวัติ
- ✅ ประวัติยังแสดงชื่อสาขาเดิมได้

---

## 🧪 การทดสอบ

### สถานการณ์ทดสอบ

1. **ลบสาขาที่ไม่มีผู้ใช้**
   - ✅ ควรลบได้
   - ✅ Snapshot 0 รายการ

2. **ลบสาขาที่มีผู้ใช้**
   - ✅ ควรลบได้
   - ✅ Snapshot ข้อมูลผู้ใช้
   - ✅ ผู้ใช้ถูกย้ายไป "ไม่ระบุสาขา"

3. **ลบสาขาที่มีประวัติเบิก/คืน**
   - ✅ ควรลบได้
   - ✅ Snapshot ประวัติ
   - ✅ ประวัติยังแสดงชื่อสาขาเดิม

4. **ลบ System Office**
   - ❌ ควรไม่สามารถลบได้

---

## 📚 เอกสารที่เกี่ยวข้อง

- `USER_DELETION_SNAPSHOT_EXPLANATION.md` - อธิบายการ snapshot เมื่อลบผู้ใช้
- `BRANCH_USER_SNAPSHOT_VERIFICATION.md` - การตรวจสอบ snapshot ผู้ใช้ประเภทสาขา
- `src/lib/office-snapshot-helpers.ts` - Helper functions สำหรับ snapshot สาขา

---

## ✅ สรุป

ระบบลบสาขาได้รับการปรับปรุงให้:
- ✅ Snapshot ชื่อสาขาล่าสุดก่อนลบเสมอ
- ✅ อนุญาตให้ลบได้แม้มีผู้ใช้อยู่
- ✅ คงชื่อสาขาไว้ในประวัติ
- ✅ ข้อมูลอื่นๆ ยังอัพเดตได้ปกติ

**การเปลี่ยนแปลงนี้ทำให้ระบบสามารถลบสาขาได้อย่างปลอดภัย โดยไม่สูญเสียข้อมูลประวัติ**

