# Snapshot Data Integrity - การรักษาความสมบูรณ์ของข้อมูล Snapshot

## ปัญหาที่พบ

เมื่อ InventoryMaster หรือ Category ถูกลบ หรือเปลี่ยนชื่อ:
- ❌ Snapshot เก่าจะแสดงข้อมูลไม่ได้ (ถ้าอ้างอิงเพียง ID)
- ❌ ไม่สามารถดูประวัติศาสตร์ได้ว่าเดิมชื่ออะไร

## วิธีแก้ไข: เก็บข้อมูลแบบ Snapshot (Self-Contained)

### หลักการ

Snapshot ต้องเก็บข้อมูลที่**สมบูรณ์และแสดงผลได้เอง** โดยไม่ต้องพึ่งพา reference อื่น

```typescript
itemDetails: {
  // ✅ อ้างอิง (สำหรับ lookup ชื่อปัจจุบัน - optional)
  masterId?: string;
  
  // ✅ ข้อมูล Snapshot (สมบูรณ์ในตัวเอง - required)
  itemName: string;         // ชื่ออุปกรณ์ ณ เวลานั้น
  categoryId: string;       // ID หมวดหมู่
  categoryName: string;     // 🆕 ชื่อหมวดหมู่ ณ เวลานั้น
  
  // สถิติ
  totalQuantity: number;
  availableQuantity: number;
  userOwnedQuantity: number;
  isLowStock: boolean;
}
```

## สถานการณ์และการจัดการ

### 1. ✅ Snapshot เดือนเดียวกัน (Re-snapshot)

**สถานการณ์**: มีการสร้าง snapshot ใหม่สำหรับเดือนเดียวกัน

**พฤติกรรม**:
- ระบบใช้ `upsert: true` จะทับข้อมูลเดิม
- ข้อมูลจะเป็นข้อมูล ณ วันที่ snapshot ใหม่

**ตัวอย่าง**:
```javascript
// Snapshot วันที่ 1 ตุลาคม
{
  itemName: "เมาส์ Logitech",
  categoryName: "อุปกรณ์คอมพิวเตอร์",
  totalQuantity: 10
}

// Re-snapshot วันที่ 15 ตุลาคม (เดือนเดียวกัน)
// → ทับข้อมูลเดิม
{
  itemName: "เมาส์ Logitech G502",  // เปลี่ยนชื่อ
  categoryName: "Peripheral",       // เปลี่ยนชื่อหมวดหมู่
  totalQuantity: 12                 // จำนวนเพิ่ม
}
```

### 2. ✅ Snapshot คนละเดือน (ข้อมูลประวัติศาสตร์)

**สถานการณ์**: Snapshot แต่ละเดือนแยกกัน

**พฤติกรรม**:
- แต่ละเดือนมี snapshot ของตัวเอง (unique: year + month)
- ถ้า item ถูกลบในเดือนหน้า → snapshot เดือนก่อนยังมีข้อมูลครบ

**ตัวอย่าง**:
```javascript
// Snapshot ตุลาคม 2568
{
  year: 2568,
  month: 10,
  itemDetails: [
    {
      masterId: "abc123",
      itemName: "เมาส์ Logitech",
      categoryName: "อุปกรณ์คอมพิวเตอร์",
      totalQuantity: 10
    }
  ]
}

// ลบ item "เมาส์ Logitech" วันที่ 5 พฤศจิกายน

// Snapshot พฤศจิกายน 2568
{
  year: 2568,
  month: 11,
  itemDetails: [
    // ไม่มี "เมาส์ Logitech" เพราะถูกลบแล้ว
  ]
}

// ✅ Snapshot ตุลาคม ยังมีข้อมูลครบ
// ✅ สามารถแสดงผลได้โดยไม่ต้องอ้างอิง master
```

### 3. ⚠️ ถ้า Item ถูกลบ

**สถานการณ์**: ลบ InventoryMaster หลังจากที่มี snapshot แล้ว

**พฤติกรรมเดิม (ปัญหา)**:
```javascript
// Snapshot เก่า
{
  masterId: "abc123",
  itemName: "เมาส์",
  categoryId: "cat_001",      // เก็บแค่ ID
  categoryName: undefined     // ❌ ไม่ได้เก็บชื่อ
}

// หลังลบ InventoryMaster และ Category
// → ไม่สามารถแสดงชื่อหมวดหมู่ได้
```

**พฤติกรรมใหม่ (แก้ไขแล้ว)**:
```javascript
// Snapshot ใหม่
{
  masterId: "abc123",
  itemName: "เมาส์ Logitech",
  categoryId: "cat_001",
  categoryName: "อุปกรณ์คอมพิวเตอร์"  // ✅ เก็บชื่อไว้
}

// หลังลบ InventoryMaster และ Category
// → ยังแสดงข้อมูลได้จาก snapshot
```

### 4. 🔄 ถ้าเปลี่ยนชื่อ

**สถานการณ์**: เปลี่ยนชื่อ item หรือ category

**พฤติกรรม**:
- Snapshot เก่า: แสดงชื่อเดิม (ประวัติศาสตร์)
- Snapshot ใหม่: แสดงชื่อใหม่
- ถ้ามี `masterId` สามารถ lookup ชื่อปัจจุบันได้

**ตัวอย่าง**:
```javascript
// Snapshot ตุลาคม
{
  masterId: "abc123",
  itemName: "เมาส์ Logitech",         // ชื่อเดิม
  categoryName: "อุปกรณ์คอมพิวเตอร์"  // ชื่อเดิม
}

// เปลี่ยนชื่อ item และ category

// Snapshot พฤศจิกายน
{
  masterId: "abc123",                 // masterId เดิม
  itemName: "เมาส์ Logitech G502",   // ชื่อใหม่
  categoryName: "Peripheral Devices"  // ชื่อใหม่
}

// เมื่อแสดงผล:
// - แสดงชื่อจาก snapshot (ประวัติศาสตร์)
// - ถ้าต้องการชื่อปัจจุบัน → ใช้ populateSnapshotItemNames()
```

## วิธีแสดงผลใน UI

### แสดงข้อมูลประวัติศาสตร์

```typescript
// แสดงข้อมูล ณ เวลาที่ snapshot (ไม่ต้องอ้างอิง)
{snapshot.itemDetails.map(item => (
  <div>
    <h3>{item.itemName}</h3>
    <p>หมวดหมู่: {item.categoryName}</p>
    <p>จำนวน: {item.totalQuantity}</p>
  </div>
))}
```

### แสดงข้อมูลปัจจุบัน (ถ้ามี masterId)

```typescript
import { populateSnapshotItemNames } from '@/lib/snapshot-populate-helpers';

// Populate ชื่อปัจจุบัน
const itemsWithCurrent = await populateSnapshotItemNames(snapshot.itemDetails);

{itemsWithCurrent.map(item => (
  <div>
    {/* ชื่อปัจจุบัน */}
    <h3>{item.currentItemName}</h3>
    
    {/* แสดงชื่อเดิมถ้าต่างกัน */}
    {item.currentItemName !== item.itemName && (
      <p className="text-sm text-gray-500">
        ชื่อเดิม: {item.itemName}
      </p>
    )}
    
    <p>หมวดหมู่: {item.categoryName}</p>
  </div>
))}
```

## การทำงานของ Snapshot System

### 1. สร้าง Snapshot (Capture ข้อมูล ณ เวลานั้น)

```typescript
// เก็บข้อมูลสมบูรณ์
itemDetails.push({
  masterId: master._id?.toString(),
  itemName: master.itemName,           // Snapshot
  categoryId: master.categoryId,
  categoryName: categoryMap.get(...),  // Snapshot
  totalQuantity: ...,
  availableQuantity: ...,
  userOwnedQuantity: ...,
  isLowStock: ...
});
```

### 2. ดูข้อมูลย้อนหลัง

- ✅ แสดงข้อมูล ณ เวลานั้นได้โดยไม่ต้องอ้างอิง
- ✅ ถ้า master ถูกลบ ยังแสดงชื่อได้
- ✅ ถ้า category ถูกลบ ยังแสดงชื่อได้

### 3. เปรียบเทียบข้อมูล

```typescript
// เปรียบเทียบ snapshot ตุลาคม กับ พฤศจิกายน
const oct = await InventorySnapshot.findOne({ year: 2568, month: 10 });
const nov = await InventorySnapshot.findOne({ year: 2568, month: 11 });

// ✅ สามารถเปรียบเทียบได้โดยไม่ต้องพึ่งพา master
```

## ข้อควรระวัง

### 1. ขนาดข้อมูล
- เก็บข้อมูลมากขึ้น → ขนาด snapshot ใหญ่ขึ้น
- แต่ยังใช้งานได้ดี (เพราะเก็บเฉพาะข้อมูลที่จำเป็น)

### 2. Consistency
- Snapshot = ข้อมูล ณ เวลานั้น
- ไม่ควรอัพเดต snapshot เก่า (ยกเว้น re-snapshot เดือนเดียวกัน)

### 3. Backward Compatibility
- `categoryName` เป็น required field ใหม่
- Snapshot เก่าที่ไม่มี `categoryName` จะ fallback เป็น 'ไม่ระบุ'
- สามารถ backfill ได้ (ถ้าต้องการ)

## สรุป

✅ **Snapshot เก็บข้อมูลสมบูรณ์ในตัวเอง**
- เก็บทั้ง ID และชื่อ
- แสดงผลได้โดยไม่ต้องอ้างอิง

✅ **รองรับการลบและเปลี่ยนชื่อ**
- ถ้า item/category ถูกลบ → snapshot เก่ายังแสดงได้
- ถ้าเปลี่ยนชื่อ → แต่ละเดือนจะแสดงชื่อ ณ เวลานั้น

✅ **ยืดหยุ่น**
- แสดงชื่อประวัติศาสตร์ (จาก snapshot)
- หรือแสดงชื่อปัจจุบัน (ผ่าน masterId + populate function)

✅ **ข้อมูลแต่ละเดือนแยกอิสระ**
- เดือนเดียวกัน → อัพเดตทับได้
- คนละเดือน → แยกกัน ไม่กระทบกัน

