# 🔐 คู่มือการตั้งค่า Super Admin Role

## ภาพรวม
ระบบได้รับการอัพเกรดเพื่อรองรับ **Super Admin Role** ซึ่งเป็น role ระดับสูงสุดในระบบ พร้อมระบบสร้าง Super Admin อัตโนมัติเมื่อเริ่มต้นใช้งาน

---

## 📋 โครงสร้าง Roles ในระบบ

```
user          → ผู้ใช้ทั่วไป (สิทธิ์พื้นฐาน)
admin         → ผู้ดูแลระบบ (จัดการระบบทั่วไป)
it_admin      → ผู้ดูแลระบบทีม IT (จัดการระบบ IT)
super_admin   → ผู้ดูแลระบบระดับสูงสุด (สิทธิ์เต็มรูปแบบ) ⭐ NEW!
```

---

## 🚀 วิธีตั้งค่า Super Admin อัตโนมัติ

### 1. คัดลอกไฟล์ Environment Variables

```bash
cp .env.example .env.local
```

### 2. แก้ไขค่าใน `.env.local`

```env
# เปิดใช้งานการสร้าง Super Admin อัตโนมัติ
ENABLE_AUTO_SUPER_ADMIN_CREATION=true

# ข้อมูล Super Admin (กรุณาเปลี่ยนข้อมูลเหล่านี้!)
SUPER_ADMIN_EMAIL=youremail@example.com
SUPER_ADMIN_PASSWORD=YourSecurePassword123!
SUPER_ADMIN_FIRST_NAME=สมชาย
SUPER_ADMIN_LAST_NAME=ใจดี
SUPER_ADMIN_NICKNAME=Admin
SUPER_ADMIN_DEPARTMENT=System Administration
SUPER_ADMIN_PHONE=081-234-5678
SUPER_ADMIN_OFFICE=UNSPECIFIED_OFFICE
```

### 3. เริ่มต้นระบบ

เมื่อเข้าใช้งาน API endpoint `/api/create-user` ครั้งแรก:

```bash
# เรียกใช้ API (ผ่าน browser หรือ curl)
curl http://localhost:3000/api/create-user
```

ระบบจะ:
1. ✅ ตรวจสอบว่ามี Admin อยู่ในระบบหรือไม่
2. ✅ ถ้าไม่มี จะสร้าง Super Admin อัตโนมัติ
3. ✅ ส่งข้อมูล Super Admin กลับมา

---

## 🔑 การเข้าสู่ระบบ Super Admin

หลังจากระบบสร้าง Super Admin แล้ว คุณสามารถเข้าสู่ระบบได้ทันที:

```
Email: [ตามที่ตั้งค่าใน SUPER_ADMIN_EMAIL]
Password: [ตามที่ตั้งค่าใน SUPER_ADMIN_PASSWORD]
```

---

## 📝 สิทธิ์ของ Super Admin

Super Admin มีสิทธิ์เท่ากับ IT Admin + สิทธิ์พิเศษเพิ่มเติม:

> **⚠️ หมายเหตุสำคัญ:** Super Admin Role สามารถมอบหมายได้โดย **Super Admin เท่านั้น** - ผู้ดูแลระบบ role อื่นๆ (Admin, IT Admin) จะไม่เห็นตัวเลือก Super Admin ใน dropdown

| ฟีเจอร์ | User | Admin | IT Admin | Super Admin |
|---------|------|-------|----------|-------------|
| ใช้งานระบบพื้นฐาน | ✅ | ✅ | ✅ | ✅ |
| จัดการ Users | ❌ | ✅ | ✅ | ✅ |
| จัดการ Inventory | ❌ | ✅ | ✅ | ✅ |
| จัดการ IT Reports | ❌ | ✅ | ✅ | ✅ |
| ดู Error Monitor | ❌ | ✅ | ✅ | ✅ |
| เข้าถึง Admin Panel | ❌ | ✅ | ✅ | ✅ |
| รับอีเมลแจ้งเตือน IT | ❌ | ❌ | ✅ | ✅ |
| isMainAdmin Flag | ❌ | ❌ | ⚠️ | ✅ |

---

## ⚙️ การสร้าง Super Admin เพิ่มเติม

### วิธีที่ 1: ผ่าน Admin Panel (แนะนำ)

> **⚠️ สำคัญ:** ต้องเข้าสู่ระบบด้วย **Super Admin เท่านั้น** เพื่อสร้าง Super Admin ใหม่

1. เข้าสู่ระบบด้วย **Super Admin** (เท่านั้น!)
2. ไปที่เมนู **Admin > Users**
3. คลิก **"+ เพิ่มผู้ใช้"**
4. กรอกข้อมูลผู้ใช้
5. เลือก **สถานะ: Super Admin** (จะปรากฏเฉพาะเมื่อ login ด้วย Super Admin)
6. คลิก **"บันทึก"**

**หมายเหตุ:** หาก login ด้วย Admin หรือ IT Admin จะไม่เห็นตัวเลือก "Super Admin" ใน dropdown

### วิธีที่ 2: ผ่าน API

```bash
POST /api/admin/users
Content-Type: application/json
Cookie: auth-token=<your-admin-token>

{
  "firstName": "John",
  "lastName": "Doe",
  "nickname": "John",
  "department": "IT Department",
  "office": "สำนักงานใหญ่",
  "phone": "081-234-5678",
  "email": "john@example.com",
  "password": "SecurePassword123!",
  "userType": "individual",
  "userRole": "super_admin"
}
```

---

## 🔒 ข้อควรระวัง Security

### 1. เปลี่ยนรหัสผ่าน Super Admin ทันที

หลังจากเข้าสู่ระบบครั้งแรก ควรเปลี่ยนรหัสผ่านทันที:
- ไปที่ **Profile Settings**
- เลือก **Change Password**
- ใส่รหัสผ่านใหม่ที่แข็งแรง

### 2. ใช้รหัสผ่านที่แข็งแรง

รหัสผ่านที่แนะนำ:
- ✅ ยาวอย่างน้อย 12 ตัวอักษร
- ✅ มีตัวพิมพ์เล็ก พิมพ์ใหญ่
- ✅ มีตอนเลข
- ✅ มีสัญลักษณ์พิเศษ
- ❌ ไม่ใช้คำที่เดาง่าย

### 3. อย่า Commit `.env.local` เข้า Git

```bash
# ตรวจสอบว่า .env.local อยู่ใน .gitignore
cat .gitignore | grep .env.local
```

### 4. จำกัดจำนวน Super Admin

แนะนำให้มี Super Admin เพียง 1-2 บัญชีเท่านั้น

---

## 🛠️ Troubleshooting

### ปัญหา: Super Admin ไม่ถูกสร้าง

**ตรวจสอบ:**
1. ✅ `ENABLE_AUTO_SUPER_ADMIN_CREATION=true` ใน `.env.local`
2. ✅ มีค่า `SUPER_ADMIN_EMAIL` และ `SUPER_ADMIN_PASSWORD`
3. ✅ ไม่มี Admin อยู่ในระบบแล้ว
4. ✅ MongoDB เชื่อมต่อสำเร็จ

**วิธีแก้:**
```bash
# ดู logs
npm run dev

# เรียก API ใหม่
curl http://localhost:3000/api/create-user
```

### ปัญหา: ลืมรหัสผ่าน Super Admin

**วิธีแก้:**
1. เชื่อมต่อกับ MongoDB โดยตรง
2. ค้นหา Super Admin user
3. อัพเดทรหัสผ่านใหม่ (ต้อง hash ด้วย bcrypt)

หรือ:
1. ลบข้อมูล users ทั้งหมดใน MongoDB
2. เรียก `/api/create-user` ใหม่

---

## 📊 การตรวจสอบ Super Admin

### ผ่าน MongoDB

```javascript
db.users.find({ userRole: 'super_admin' })
```

### ผ่าน API (ต้อง login ก่อน)

```bash
GET /api/admin/users
Cookie: auth-token=<your-admin-token>

# Response จะแสดง users ทั้งหมด รวมถึง Super Admin
```

---

## 🎯 Best Practices

1. **✅ ใช้ email จริง** - เพื่อรับการแจ้งเตือนจากระบบ
2. **✅ เก็บข้อมูล Super Admin ไว้ที่ปลอดภัย** - บันทึกไว้ใน password manager
3. **✅ Enable 2FA** (ถ้ามี) - เพิ่มความปลอดภัยอีกชั้น
4. **✅ Log การทำงาน** - ติดตามการใช้งาน Super Admin
5. **✅ Review สิทธิ์เป็นประจำ** - ตรวจสอบว่ามีคนที่ไม่ควรมีสิทธิ์หรือไม่

---

## 📞 ติดต่อ Support

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อ:
- 📧 Email: it-support@yourcompany.com
- 📱 Phone: 02-XXX-XXXX
- 💬 Line: @yoursupport

---

## 📅 Change Log

### Version 2.0 (2024)
- ✨ เพิ่ม Super Admin Role
- ✨ ระบบสร้าง Super Admin อัตโนมัติ
- ✨ อัพเดท permission checks ทั้งระบบ
- ✨ เพิ่ม UI สำหรับจัดการ Super Admin

---

**หมายเหตุ:** คู่มือนี้อัพเดทล่าสุด วันที่ 12 พฤศจิกายน 2025

